<?php
/**
 * Project Overrides uninstall handler.
 *
 * Data is deliberately retained. Project CSS should never be deleted merely
 * because the plugin is temporarily removed.
 *
 * @package ProjectOverrides
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
